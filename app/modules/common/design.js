
// alert('test');
// $('.switch-description').css('cursor','pointer');


// $('.switch-description').click(function(){
        // var $body = $(this).next();
        // $('.summary').not($body).removeClass('active');
        // $(this).next(.toggleClass('active');
    // });

// $('.summary-switch').click(function(){
        // var $body = $(this).next();
        // $('.summary').not($body).removeClass('active');
        // $($body).toggleClass('active');
    // });
function MainController($scope) {
  $scope.toggle = true;
}